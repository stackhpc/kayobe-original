---
features:
  - |
    Adds the ``kolla_ansible_venv_site_packages`` configuration option. This
    allows you to use system site packages when creating the kolla-ansible
    virtualenv. The default is ``false``.
